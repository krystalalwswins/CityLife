# hm-dianping-vue

基于 `Vue3 + Vite + Element Plus + Axios + Pinia` 的黑马点评前端示例项目。

## 启动

```bash
npm install
npm run dev
```

默认访问地址：

```bash
http://localhost:5173
```

## 说明

- 当前版本全部使用 `src/mock/` 下的模拟数据
- 不依赖后端接口即可运行
- 已包含首页、分类页、商家详情、个人中心、AI 美食助手
